# NOTES.md

## 选做 A：出库单 + 库存扣减 — 并发控制方案

### 方案选择：SQL 原子更新（无悲观锁）

核心扣减语句：
```sql
UPDATE inventory
SET quantity = quantity - :delta, updated_at = CURRENT_TIMESTAMP
WHERE product_id = :pid AND location_code = :loc AND quantity >= :delta
```

### 为什么选这个方案

**1. 对比三种主流方案**

| 方案 | 原理 | 优点 | 缺点 | 适用场景 |
|------|------|------|------|----------|
| 悲观锁 `SELECT ... FOR UPDATE` | 读取时加行锁，其他事务等待 | 简单直接，强一致性 | 锁等待降低吞吐，并发高时大量请求阻塞 | 中低并发、强一致性要求 |
| 乐观锁 `@Version` | 版本号检测冲突，冲突时重试 | 无锁等待，高吞吐 | 冲突频繁时重试开销大，库存是热点数据冲突率极高 | 中高并发、冲突较少 |
| **SQL 原子更新** | 一条 UPDATE 同时完成校验+扣减 | 无锁竞争、无重试、一条 SQL 完成 | 只返回 affected rows，需额外查询获取友好错误信息 | **高并发、简单扣减** |

**2. 为什么不用悲观锁兜底**

原方案"原子更新 + 悲观锁兜底"存在概念矛盾：
- 原子更新的 `WHERE quantity >= :delta` 条件由数据库行级锁保证原子性，UPDATE 执行时自动对目标行加排他锁，其他并发 UPDATE 必须等待该行锁释放后才能执行
- 悲观锁 `SELECT ... FOR UPDATE` 解决的是同一个问题（防止并发修改同一行），两者叠加只会增加复杂度，不会增加安全性
- 悲观锁的额外代价：需要先 SELECT 再 UPDATE，多一次数据库交互，且 SELECT FOR UPDATE 在无匹配行时不加锁（对"商品未入库到该库位"的场景无效）

**3. 为什么不用乐观锁**

库存扣减是热点数据——多个出库请求频繁操作同一 `product_id + location_code` 的库存行：
- 乐观锁在冲突时需要重试，库存行的冲突率远高于普通数据
- 每次重试都要重新加载实体、比较版本号、再次尝试更新，在高并发下重试次数可能很多
- 原子更新一次就完成，不需要重试循环

**4. 原子更新的唯一不足及应对**

原子更新返回 `affected rows = 0` 时，无法区分"库存不足"和"记录不存在"两种情况。
应对方式：扣减失败后立即查询库存记录，获取当前余量用于构造友好错误信息：
```
库存不足：商品[蓝牙耳机 Pro]在库位[WH-A-01-01]仅剩 30，需出库 50
```

### 并发安全保证机制

**1. 防超卖**
- `WHERE quantity >= :delta` 条件确保只有库存充足时才能扣减成功
- 数据库行级排他锁保证同一行的并发 UPDATE 串行执行，不会出现"两个事务都读到 100、各扣 50、最终写入 50"的丢失更新

**2. 防事务部分失败**
- `@Transactional` 保证整个出库单创建在同一个事务中
- 如果多明细项中第 N 项库存不足导致 `affected rows = 0`，抛出 BusinessException，整个事务回滚，前面已扣减的库存恢复原值

**3. 防单号竞态**
- 出库单号生成方法使用 `synchronized` 关键字
- 本项目为单实例部署（Spring Boot 内嵌 Tomcat），`synchronized` 足够保证单号唯一
- 如果未来需要多实例部署，应改为数据库序列或 Redis 原子递增

**4. 防明细重复**
- 出库前校验 `productId + locationCode` 组合去重，拒绝同一商品在同一库位出现两条明细

### 测试验证

7 个测试用例全部通过，关键测试：

| 测试 | 验证点 |
|------|--------|
| 正常出库 | 库存正确扣减 |
| 库存不足 | 返回友好错误，库存不变 |
| 商品不存在 | 抛出 BusinessException |
| 库位不存在 | 抛出 BusinessException |
| 重复明细 | 拒绝同一商品+库位组合 |
| 部分失败回滚 | 第二项失败时，第一项已扣减的库存恢复 |
| **并发超卖** | 10 线程各出库 15（总计需 150），库存仅 100，最终库存不为负，成功次数 * 15 = 实际扣减量 |